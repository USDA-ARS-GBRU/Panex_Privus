"""Utilities subpackage for Panex Privus.

Modules:
    logging     — logger configuration (implemented)
    validation  — input validation helpers (Phase 2)
    parallel    — thread/process pool helpers (Phase 2)
    metrics     — runtime statistics accumulators (Phase 2)
    misc        — miscellaneous helpers (Phase 2)
"""
