"""Report subpackage — interpretation and ranking engine.

Converts raw scan and compare outputs into ranked summaries and
human-readable Markdown/HTML reports.

Modules:
    summary   — TSV ranked summaries and strictness distributions (Phase 2)
    markdown  — Markdown report generator (Phase 2)
    html      — HTML report generator (Phase 2)
"""
